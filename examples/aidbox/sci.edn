{ns aidbox.sci

 import #{sty}

 case
 {:zen/tags #{sty/case}
  :title "Test case"
  :data {:my-id "pt-1"}

  :steps
  {:sci
   {:action {:type sty/http
             :method :get :url (sty/url "Patient" (get-in sty/case [:data :my-id]))}
    :matches {:type sty/matcho
              :body {:id (get-in sty/case [:data :my-id])}}}}}

 }
